@model DevWeb0306.Models.Curso
@{
    ViewData["Title"] = "Detalhes do Curso";
}
<div class="page-header">
    <h1>Detalhes do Curso</h1>
    <p class="page-subtitle">Informações completas do curso</p>
</div>

<div class="card">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Nome)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Nome)
            </dd>
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Eixo)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Eixo.Nome)
            </dd>
        </dl>

@*         @if (Model.Materias != null && Model.Materias.Any())
        {
            <hr />
            <h5 class="mt-4 mb-3">Matérias Associadas</h5>
            <div class="table-container">
                <table class="table table-sm">
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th>Professor</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var materia in Model.Materias)
                        {
                            <tr>
                                <td>@materia.Nome</td>
                                <td>@(materia.Professor?.Nome ?? "Não atribuído")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        } *@

        <div class="d-flex justify-content-between mt-4">
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Voltar
            </a>
            <div>
                <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-outline-primary">
                    <i class="bi bi-pencil"></i> Editar
                </a>
            </div>
        </div>
    </div>
</div>