@model DevWeb0306.Models.Eixo
@{
    ViewData["Title"] = "Detalhes do Eixo";
}
<div class="page-header">
    <h1>Detalhes do Eixo</h1>
    <p class="page-subtitle">Informações completas do eixo temático</p>
</div>

<div class="card">
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Nome)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Nome)
            </dd>
@* 
            @if (!string.IsNullOrEmpty(Model.Descricao))
            {
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Descricao)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Descricao)
                </dd>
            } *@
        </dl>

@*         @if (Model.Cursos != null && Model.Cursos.Any())
        {
            <hr />
            <h5 class="mt-4 mb-3">Cursos Associados</h5>
            <div class="table-container">
                <table class="table table-sm">
                    <thead>
                        <tr>
                            <th>Nome</th>
                            <th class="text-end">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var curso in Model.Cursos)
                        {
                            <tr>
                                <td>@curso.Nome</td>
                                <td class="text-end">
                                    <div class="table-actions">
                                        <a asp-controller="Cursos" asp-action="Details" asp-route-id="@curso.Id" class="btn btn-sm btn-outline-primary">
                                            <i class="bi bi-info-circle"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div> *@
@*         }
        else
        {
            <div class="alert alert-info mt-4">
                <i class="bi bi-info-circle"></i> Este eixo não possui cursos associados.
            </div>
        }
 *@
        <div class="d-flex justify-content-between mt-4">
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left"></i> Voltar
            </a>
            <div>
                <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-outline-primary">
                    <i class="bi bi-pencil"></i> Editar
                </a>
            </div>
        </div>
    </div>
</div>